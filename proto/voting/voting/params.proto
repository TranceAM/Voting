syntax = "proto3";
package tranceam.voting.voting;

import "gogoproto/gogo.proto";

option go_package = "github.com/TranceAM/voting/x/voting/types";

// Params defines the parameters for the module.
message Params {
  option (gogoproto.goproto_stringer) = false;
  
}
